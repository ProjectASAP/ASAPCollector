# Overlay: 10 edge agents + 10 workload producers. Auto-generated
# by `./gen-agents.sh 10 > agents-N10.yml`. Do not edit by hand;
# edit `gen-agents.sh` if the per-agent template changes.
#
# Per-replica flow:
#
#   fake-exporter-i ──OTLP──▶ agent-i ──OTLP──▶ gateway ──promRW──▶ backend
#
# Each agent mounts `asap-otel-agent.yaml` and runs DDSketch + HLL
# on the pipeline, so the bytes reaching the gateway are already
# sketched (bandwidth-reduction signal scales with N).

x-agent: &agent-base
  image: asap/asap-otel:dev
  depends_on:
    - gateway
    - controller
  # The opampextension's `remote_config_path` (ASAPCollector#391)
  # writes the controller-pushed RemoteConfig back to
  # `/etc/otel/config.yaml` and exits, expecting Docker to restart
  # the container so the new config takes effect. Both of the
  # following are required for that flow to work:
  #   - the mount must be RW (no `:ro`) so the on-disk write succeeds
  #   - `restart: unless-stopped` so Docker brings the agent back up
  # See /mydata/mvp-smoke-test/compose/smoke-overlay.yml for the
  # working reference that established this contract.
  restart: unless-stopped
  command:
    - "--config=/etc/otel/config.yaml"
  volumes:
    # Default baseline = B2 full-sketch. Override via env var
    # AGENT_CONFIG to mount a different pipeline yaml — the
    # paper baselines' compose overlays (baseline-b*.yml) set
    # it before `docker compose up`. Compose expands the
    # ${VAR:-default} syntax at container start.
    - ../configs/${AGENT_CONFIG:-asap-otel-agent-b2-full.yaml}:/etc/otel/config.yaml
  deploy:
    resources:
      limits:
        # DDSketch + HLL state at cardinality=1000 runs ~400MB;
        # 1 GiB leaves headroom for burst windows. Bump further
        # for higher-cardinality workloads.
        cpus: "1.0"
        memory: 1024M

x-producer: &producer-base
  image: asap/fake-exporter:dev

services:
  # The base.yml fake-exporter service is overridden here to
  # target agent-1 so its definition stays meaningful.
  fake-exporter:
    environment:
      EXPORTER_TARGET: "agent-1:4317"
      # workload-sweep knobs. Defaults match the N=1
      # smoke-test. Override at bring-up:
      #
      #   EXPORTER_RATE=10000 EXPORTER_CARDINALITY=5000 \
      #     docker compose -f base.yml -f agents-N1.yml up -d
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  agent-1:
    <<: *agent-base
    hostname: agent-1
    environment:
      AGENT_ID: "agent-1"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-2:
    <<: *agent-base
    hostname: agent-2
    environment:
      AGENT_ID: "agent-2"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-3:
    <<: *agent-base
    hostname: agent-3
    environment:
      AGENT_ID: "agent-3"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-4:
    <<: *agent-base
    hostname: agent-4
    environment:
      AGENT_ID: "agent-4"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-5:
    <<: *agent-base
    hostname: agent-5
    environment:
      AGENT_ID: "agent-5"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-6:
    <<: *agent-base
    hostname: agent-6
    environment:
      AGENT_ID: "agent-6"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-7:
    <<: *agent-base
    hostname: agent-7
    environment:
      AGENT_ID: "agent-7"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-8:
    <<: *agent-base
    hostname: agent-8
    environment:
      AGENT_ID: "agent-8"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-9:
    <<: *agent-base
    hostname: agent-9
    environment:
      AGENT_ID: "agent-9"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  agent-10:
    <<: *agent-base
    hostname: agent-10
    environment:
      AGENT_ID: "agent-10"
      CONTROLLER_OPAMP_URL: "ws://controller:4320/v1/opamp"
      SKETCH_RUNTIME_GRPC_ENDPOINT: "http://controller:4321"
      RUST_LOG: "info"
      # B4 tunable-window baseline reads ${env:SKETCH_WINDOW}
      # in asap-otel-agent-b4-tunable.yaml. Other baselines
      # ignore this env. Default 60s.
      SKETCH_WINDOW: "${SKETCH_WINDOW:-60s}"

  fake-exporter-2:
    <<: *producer-base
    depends_on:
      - agent-2
    environment:
      EXPORTER_TARGET: "agent-2:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-3:
    <<: *producer-base
    depends_on:
      - agent-3
    environment:
      EXPORTER_TARGET: "agent-3:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-4:
    <<: *producer-base
    depends_on:
      - agent-4
    environment:
      EXPORTER_TARGET: "agent-4:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-5:
    <<: *producer-base
    depends_on:
      - agent-5
    environment:
      EXPORTER_TARGET: "agent-5:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-6:
    <<: *producer-base
    depends_on:
      - agent-6
    environment:
      EXPORTER_TARGET: "agent-6:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-7:
    <<: *producer-base
    depends_on:
      - agent-7
    environment:
      EXPORTER_TARGET: "agent-7:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-8:
    <<: *producer-base
    depends_on:
      - agent-8
    environment:
      EXPORTER_TARGET: "agent-8:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-9:
    <<: *producer-base
    depends_on:
      - agent-9
    environment:
      EXPORTER_TARGET: "agent-9:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"

  fake-exporter-10:
    <<: *producer-base
    depends_on:
      - agent-10
    environment:
      EXPORTER_TARGET: "agent-10:4317"
      EXPORTER_RATE: "${EXPORTER_RATE:-1000}"
      EXPORTER_CARDINALITY: "${EXPORTER_CARDINALITY:-1000}"
