# Baseline B4 — Delta sketch with tunable time granularity.
#
# Baseline-sweep: same semantics as B3 but
# `window_duration` is controlled by the `SKETCH_WINDOW` env var,
# so a single agent image + config can sweep 5s / 30s / 60s /
# 300s without rebuilding.
#
# Use via the compose overlay:
#
#   SKETCH_WINDOW=10s docker compose -f base.yml \
#     -f agents-N1.yml -f baseline-b4.yml up -d
#
# The `${env:SKETCH_WINDOW}` expansion is the OTel collector's
# standard config-var syntax. An unset var triggers a startup
# error — set a default in the overlay. Each agent gets its own
# value so N>1 runs can mix window durations if ever useful.
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  ddsketch:
    mode: window
    window_duration: ${env:SKETCH_WINDOW}
    transmit_sketch: true
    delta_transmission: true
    delta_threshold: 1
    enable_self_monitoring: true
    quantiles: [0.5, 0.9, 0.99]
    metric_suffix: "_quantile"

  HLL:
    mode: window
    window_duration: ${env:SKETCH_WINDOW}
    transmit_sketch: true
    delta_transmission: true
    enable_self_monitoring: true

  batch:
    timeout: 1s
    send_batch_size: 1024

exporters:
  otlp/gateway:
    endpoint: gateway:4317
    tls:
      insecure: true

service:
  pipelines:
    # Per-sketch pipelines — see sketchcol-agent-b3-delta.yaml for
    # why chaining `[ddsketch, HLL, batch]` silently drops everything.
    # Short version: both sketch processors run in window mode where
    # ConsumeMetrics accumulates and `return nil`s without forwarding,
    # so the second sketch processor never sees the upstream's window
    # emission as something it can re-accumulate.
    metrics/ddsketch:
      receivers: [otlp]
      processors: [ddsketch, batch]
      exporters: [otlp/gateway]
    metrics/hll:
      receivers: [otlp]
      processors: [HLL, batch]
      exporters: [otlp/gateway]

  telemetry:
    logs:
      level: info
    metrics:
      level: detailed
      readers:
        - pull:
            exporter:
              prometheus:
                host: 0.0.0.0
                port: 8890
