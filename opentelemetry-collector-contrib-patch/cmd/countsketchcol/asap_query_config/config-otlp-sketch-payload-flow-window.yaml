# OTLP Sketch Payload Flow - Window mode - Local testing
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:53217
      http:
        endpoint: 0.0.0.0:53218

processors:
  countsketch:
    mode: window
    window_size: 5s
    epsilon: 0.01
    delta: 0.99
    transmit_sketch: true
    drop_original: true

exporters:
  otlphttp:
    endpoint: http://localhost:4318
    encoding: proto
    compression: gzip
service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [countsketch]
      exporters: [otlphttp]
  telemetry:
    logs:
      level: debug
    resource:
      service.name: countsketchcol-sketch-payload-flow-window-local
