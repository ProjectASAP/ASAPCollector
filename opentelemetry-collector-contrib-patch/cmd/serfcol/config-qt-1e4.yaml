receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
        max_recv_msg_size_mib: 64

processors:
  serf:
    compression: qt
    window_interval: 10s
    # Tighter error bound (paper sweep value: 1e-4).
    # Qt step = 2 * 1e-4 * 0.999 ≈ 0.0001998. Smaller step → more quantization
    # levels → longer Elias Gamma codes → worse compression.
    max_diff: 1.0e-4
    drop_original: false
    local_dir: /tmp/serf-qt-bench-1e4

  batch:
    send_batch_size: 5000
    timeout: 200ms

exporters:
  prometheus:
    endpoint: 0.0.0.0:8889
  debug:
    verbosity: basic

service:
  telemetry:
    metrics:
      readers:
        - pull:
            exporter:
              prometheus:
                host: 0.0.0.0
                port: 8888

  pipelines:
    metrics:
      receivers: [otlp]
      processors: [serf, batch]
      exporters: [prometheus, debug]
