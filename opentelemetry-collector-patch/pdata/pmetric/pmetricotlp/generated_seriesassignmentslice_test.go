// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package pmetricotlp

import (
	"testing"
	"unsafe"

	"github.com/stretchr/testify/assert"

	"go.opentelemetry.io/collector/pdata/internal"
)

func TestSeriesAssignmentSlice(t *testing.T) {
	es := NewSeriesAssignmentSlice()
	assert.Equal(t, 0, es.Len())
	es = newSeriesAssignmentSlice(&[]*internal.SeriesAssignment{}, internal.NewState())
	assert.Equal(t, 0, es.Len())

	emptyVal := NewSeriesAssignment()
	testVal := generateTestSeriesAssignment()
	for i := 0; i < 7; i++ {
		es.AppendEmpty()
		assert.Equal(t, emptyVal, es.At(i))
		(*es.orig)[i] = internal.GenTestSeriesAssignment()
		assert.Equal(t, testVal, es.At(i))
	}
	assert.Equal(t, 7, es.Len())
}

func TestSeriesAssignmentSliceReadOnly(t *testing.T) {
	sharedState := internal.NewState()
	sharedState.MarkReadOnly()
	es := newSeriesAssignmentSlice(&[]*internal.SeriesAssignment{}, sharedState)
	assert.Equal(t, 0, es.Len())
	assert.Panics(t, func() { es.AppendEmpty() })
	assert.Panics(t, func() { es.EnsureCapacity(2) })
	es2 := NewSeriesAssignmentSlice()
	es.CopyTo(es2)
	assert.Panics(t, func() { es2.CopyTo(es) })
	assert.Panics(t, func() { es.MoveAndAppendTo(es2) })
	assert.Panics(t, func() { es2.MoveAndAppendTo(es) })
}

func TestSeriesAssignmentSlice_CopyTo(t *testing.T) {
	dest := NewSeriesAssignmentSlice()
	src := generateTestSeriesAssignmentSlice()
	src.CopyTo(dest)
	assert.Equal(t, generateTestSeriesAssignmentSlice(), dest)
	dest.CopyTo(dest)
	assert.Equal(t, generateTestSeriesAssignmentSlice(), dest)
}

func TestSeriesAssignmentSlice_EnsureCapacity(t *testing.T) {
	es := generateTestSeriesAssignmentSlice()

	// Test ensure smaller capacity.
	const ensureSmallLen = 4
	es.EnsureCapacity(ensureSmallLen)
	assert.Less(t, ensureSmallLen, es.Len())
	assert.Equal(t, es.Len(), cap(*es.orig))
	assert.Equal(t, generateTestSeriesAssignmentSlice(), es)

	// Test ensure larger capacity
	const ensureLargeLen = 9
	es.EnsureCapacity(ensureLargeLen)
	assert.Less(t, generateTestSeriesAssignmentSlice().Len(), ensureLargeLen)
	assert.Equal(t, ensureLargeLen, cap(*es.orig))
	assert.Equal(t, generateTestSeriesAssignmentSlice(), es)
}

func TestSeriesAssignmentSlice_MoveAndAppendTo(t *testing.T) {
	// Test MoveAndAppendTo to empty
	expectedSlice := generateTestSeriesAssignmentSlice()
	dest := NewSeriesAssignmentSlice()
	src := generateTestSeriesAssignmentSlice()
	src.MoveAndAppendTo(dest)
	assert.Equal(t, generateTestSeriesAssignmentSlice(), dest)
	assert.Equal(t, 0, src.Len())
	assert.Equal(t, expectedSlice.Len(), dest.Len())

	// Test MoveAndAppendTo empty slice
	src.MoveAndAppendTo(dest)
	assert.Equal(t, generateTestSeriesAssignmentSlice(), dest)
	assert.Equal(t, 0, src.Len())
	assert.Equal(t, expectedSlice.Len(), dest.Len())

	// Test MoveAndAppendTo not empty slice
	generateTestSeriesAssignmentSlice().MoveAndAppendTo(dest)
	assert.Equal(t, 2*expectedSlice.Len(), dest.Len())
	for i := 0; i < expectedSlice.Len(); i++ {
		assert.Equal(t, expectedSlice.At(i), dest.At(i))
		assert.Equal(t, expectedSlice.At(i), dest.At(i+expectedSlice.Len()))
	}

	dest.MoveAndAppendTo(dest)
	assert.Equal(t, 2*expectedSlice.Len(), dest.Len())
	for i := 0; i < expectedSlice.Len(); i++ {
		assert.Equal(t, expectedSlice.At(i), dest.At(i))
		assert.Equal(t, expectedSlice.At(i), dest.At(i+expectedSlice.Len()))
	}
}

func TestSeriesAssignmentSlice_RemoveIf(t *testing.T) {
	// Test RemoveIf on empty slice
	emptySlice := NewSeriesAssignmentSlice()
	emptySlice.RemoveIf(func(el SeriesAssignment) bool {
		t.Fail()
		return false
	})

	// Test RemoveIf
	filtered := generateTestSeriesAssignmentSlice()
	pos := 0
	filtered.RemoveIf(func(el SeriesAssignment) bool {
		pos++
		return pos%2 == 1
	})
	assert.Equal(t, 2, filtered.Len())
}

func TestSeriesAssignmentSlice_RemoveIfAll(t *testing.T) {
	got := generateTestSeriesAssignmentSlice()
	got.RemoveIf(func(el SeriesAssignment) bool {
		return true
	})
	assert.Equal(t, 0, got.Len())
}

func TestSeriesAssignmentSliceAll(t *testing.T) {
	ms := generateTestSeriesAssignmentSlice()
	assert.NotEmpty(t, ms.Len())

	var c int
	for i, v := range ms.All() {
		assert.Equal(t, ms.At(i), v, "element should match")
		c++
	}
	assert.Equal(t, ms.Len(), c, "All elements should have been visited")
}

func TestSeriesAssignmentSlice_Sort(t *testing.T) {
	es := generateTestSeriesAssignmentSlice()
	es.Sort(func(a, b SeriesAssignment) bool {
		return uintptr(unsafe.Pointer(a.orig)) < uintptr(unsafe.Pointer(b.orig))
	})
	for i := 1; i < es.Len(); i++ {
		assert.Less(t, uintptr(unsafe.Pointer(es.At(i-1).orig)), uintptr(unsafe.Pointer(es.At(i).orig)))
	}
	es.Sort(func(a, b SeriesAssignment) bool {
		return uintptr(unsafe.Pointer(a.orig)) > uintptr(unsafe.Pointer(b.orig))
	})
	for i := 1; i < es.Len(); i++ {
		assert.Greater(t, uintptr(unsafe.Pointer(es.At(i-1).orig)), uintptr(unsafe.Pointer(es.At(i).orig)))
	}
}

func generateTestSeriesAssignmentSlice() SeriesAssignmentSlice {
	ms := NewSeriesAssignmentSlice()
	*ms.orig = internal.GenTestSeriesAssignmentPtrSlice()
	return ms
}
