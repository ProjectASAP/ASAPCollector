// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"strconv"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	gootlpmetrics "go.opentelemetry.io/proto/slim/otlp/metrics/v1"
	"google.golang.org/protobuf/proto"

	"go.opentelemetry.io/collector/featuregate"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

func TestCopyHLLSketch(t *testing.T) {
	for name, src := range genTestEncodingValuesHLLSketch() {
		for _, pooling := range []bool{true, false} {
			t.Run(name+"/Pooling="+strconv.FormatBool(pooling), func(t *testing.T) {
				prevPooling := UseProtoPooling.IsEnabled()
				require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), pooling))
				defer func() {
					require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), prevPooling))
				}()

				dest := NewHLLSketch()
				CopyHLLSketch(dest, src)
				assert.Equal(t, src, dest)
				CopyHLLSketch(dest, dest)
				assert.Equal(t, src, dest)
			})
		}
	}
}

func TestCopyHLLSketchSlice(t *testing.T) {
	src := []HLLSketch{}
	dest := []HLLSketch{}
	// Test CopyTo empty
	dest = CopyHLLSketchSlice(dest, src)
	assert.Equal(t, []HLLSketch{}, dest)

	// Test CopyTo larger slice
	src = GenTestHLLSketchSlice()
	dest = CopyHLLSketchSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchSlice(), dest)

	// Test CopyTo same size slice
	dest = CopyHLLSketchSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchSlice(), dest)

	// Test CopyTo smaller size slice
	dest = CopyHLLSketchSlice(dest, []HLLSketch{})
	assert.Len(t, dest, 0)

	// Test CopyTo larger slice with enough capacity
	dest = CopyHLLSketchSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchSlice(), dest)
}

func TestCopyHLLSketchPtrSlice(t *testing.T) {
	src := []*HLLSketch{}
	dest := []*HLLSketch{}
	// Test CopyTo empty
	dest = CopyHLLSketchPtrSlice(dest, src)
	assert.Equal(t, []*HLLSketch{}, dest)

	// Test CopyTo larger slice
	src = GenTestHLLSketchPtrSlice()
	dest = CopyHLLSketchPtrSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchPtrSlice(), dest)

	// Test CopyTo same size slice
	dest = CopyHLLSketchPtrSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchPtrSlice(), dest)

	// Test CopyTo smaller size slice
	dest = CopyHLLSketchPtrSlice(dest, []*HLLSketch{})
	assert.Len(t, dest, 0)

	// Test CopyTo larger slice with enough capacity
	dest = CopyHLLSketchPtrSlice(dest, src)
	assert.Equal(t, GenTestHLLSketchPtrSlice(), dest)
}

func TestMarshalAndUnmarshalJSONHLLSketchUnknown(t *testing.T) {
	iter := json.BorrowIterator([]byte(`{"unknown": "string"}`))
	defer json.ReturnIterator(iter)
	dest := NewHLLSketch()
	dest.UnmarshalJSON(iter)
	require.NoError(t, iter.Error())
	assert.Equal(t, NewHLLSketch(), dest)
}

func TestMarshalAndUnmarshalJSONHLLSketch(t *testing.T) {
	for name, src := range genTestEncodingValuesHLLSketch() {
		for _, pooling := range []bool{true, false} {
			t.Run(name+"/Pooling="+strconv.FormatBool(pooling), func(t *testing.T) {
				prevPooling := UseProtoPooling.IsEnabled()
				require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), pooling))
				defer func() {
					require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), prevPooling))
				}()

				stream := json.BorrowStream(nil)
				defer json.ReturnStream(stream)
				src.MarshalJSON(stream)
				require.NoError(t, stream.Error())

				iter := json.BorrowIterator(stream.Buffer())
				defer json.ReturnIterator(iter)
				dest := NewHLLSketch()
				dest.UnmarshalJSON(iter)
				require.NoError(t, iter.Error())

				assert.Equal(t, src, dest)
				DeleteHLLSketch(dest, true)
			})
		}
	}
}

func TestMarshalAndUnmarshalProtoHLLSketchFailing(t *testing.T) {
	for name, buf := range genTestFailingUnmarshalProtoValuesHLLSketch() {
		t.Run(name, func(t *testing.T) {
			dest := NewHLLSketch()
			require.Error(t, dest.UnmarshalProto(buf))
		})
	}
}

func TestMarshalAndUnmarshalProtoHLLSketchUnknown(t *testing.T) {
	dest := NewHLLSketch()
	// message Test { required int64 field = 1313; } encoding { "field": "1234" }
	require.NoError(t, dest.UnmarshalProto([]byte{0x88, 0x52, 0xD2, 0x09}))
	assert.Equal(t, NewHLLSketch(), dest)
}

func TestMarshalAndUnmarshalProtoHLLSketch(t *testing.T) {
	for name, src := range genTestEncodingValuesHLLSketch() {
		for _, pooling := range []bool{true, false} {
			t.Run(name+"/Pooling="+strconv.FormatBool(pooling), func(t *testing.T) {
				prevPooling := UseProtoPooling.IsEnabled()
				require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), pooling))
				defer func() {
					require.NoError(t, featuregate.GlobalRegistry().Set(UseProtoPooling.ID(), prevPooling))
				}()

				buf := make([]byte, src.SizeProto())
				gotSize := src.MarshalProto(buf)
				assert.Equal(t, len(buf), gotSize)

				dest := NewHLLSketch()
				require.NoError(t, dest.UnmarshalProto(buf))

				assert.Equal(t, src, dest)
				DeleteHLLSketch(dest, true)
			})
		}
	}
}

func TestMarshalAndUnmarshalProtoViaProtobufHLLSketch(t *testing.T) {
	for name, src := range genTestEncodingValuesHLLSketch() {
		t.Run(name, func(t *testing.T) {
			buf := make([]byte, src.SizeProto())
			gotSize := src.MarshalProto(buf)
			assert.Equal(t, len(buf), gotSize)

			goDest := &gootlpmetrics.HLLSketch{}
			require.NoError(t, proto.Unmarshal(buf, goDest))

			goBuf, err := proto.Marshal(goDest)
			require.NoError(t, err)

			dest := NewHLLSketch()
			require.NoError(t, dest.UnmarshalProto(goBuf))
			assert.Equal(t, src, dest)
		})
	}
}

func genTestFailingUnmarshalProtoValuesHLLSketch() map[string][]byte {
	return map[string][]byte{
		"invalid_field":                          {0x02},
		"DataPoints/wrong_wire_type":             {0xc},
		"DataPoints/missing_value":               {0xa},
		"AggregationTemporality/wrong_wire_type": {0x14},
		"AggregationTemporality/missing_value":   {0x10},
		"Precision/wrong_wire_type":              {0x1c},
		"Precision/missing_value":                {0x18},
	}
}

func genTestEncodingValuesHLLSketch() map[string]*HLLSketch {
	return map[string]*HLLSketch{
		"empty":                       NewHLLSketch(),
		"DataPoints/test":             {DataPoints: []*HLLSketchDataPoint{{}, GenTestHLLSketchDataPoint()}},
		"AggregationTemporality/test": {AggregationTemporality: AggregationTemporality(13)},
		"Precision/test":              {Precision: uint32(13)},
	}
}
