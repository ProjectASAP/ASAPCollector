# OpenTelemetry Collector Plugins/Patches

Copies of modified files from the vendored `opentelemetry-collector` tree are stored here by `backup_collector_plugins.sh` for easier review and portability. Run `restore_collector_plugins.sh` to sync them back into the repository.
